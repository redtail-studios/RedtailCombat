# Redtail Intelligence Scraper — Dependencies
# Install with: pip install -r requirements.txt

# --- Web server (dashboard) ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# --- Claude AI (report generation) ---
anthropic>=0.97.0

# --- Scraping ---
praw>=7.7.0                  # Reddit API (free, needs API key)
requests>=2.31.0             # HTTP requests
beautifulsoup4>=4.12.0       # HTML parsing
google-play-scraper>=1.2.4   # Google Play reviews (no key needed)
app-store-scraper>=0.3.5     # Apple App Store reviews (no key needed)

# --- Analysis ---
vaderSentiment>=3.3.2        # Sentiment analysis (local, no API)
pandas>=2.1.0                # Data manipulation
tqdm>=4.66.0                 # Progress bars

# --- Report Generation ---
jinja2>=3.1.3                # HTML report templating

# --- Utilities ---
python-dotenv>=1.0.0         # Load .env config file
